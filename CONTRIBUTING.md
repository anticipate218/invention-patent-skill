# 贡献指南

欢迎通过 Issue 或 Pull Request 改进本技能。本指南说明具体的贡献原则与流程。

---

## 一、内容原则（最重要）

本技能是**规则追踪项目**，其价值在于"准确记录官方规则"而非"给建议"。贡献必须遵循：

### 1.1 每条规定必须标注来源可信度

- **`[官方]`**：来自《专利法》《专利法实施细则》《专利审查指南》原文，或 CNIPA（国家知识产权局）官网文件。需附链接与条款号（例：`专利法第 26 条第 4 款`）。
- **`[半官方]`**：来自 CNIPA 官方问答、审查业务指导、官方公报、公开的审查操作规程。需注明出处与检索日期。
- **`[社区]`**：来自公开的代理实务书籍、代理机构技术文章、公开判例解读。需注明出处，并说明"非官方口径"。

未标注来源的规定不会被合并。**法律与费用数字必须落到 `[官方]` 或 `[半官方]`。**

### 1.2 "官方未公开"的项目必须明确说明

例如：

- ✅ 正确："《专利审查指南》只给出创造性的三步法判断框架，从未公布量化评分表；实际授权判断由审查员个案裁量。"
- ❌ 错误："创造性判断中，技术启示占 40%、技术效果占 30%、商业成功占 30%。"（编造）
- ❌ 错误："发明专利平均授权率 55%。"（未给年份、未给口径、未给来源）

如果只能找到社区流传的说法，必须加 `[社区]` 并说明"非官方"。

### 1.3 规则变更的处理方式

专利法、实施细则、审查指南与收费标准都会修订。报告变更时请：

1. **提供新旧文本对照**：附新版链接 + 具体条款号与原文对比。
2. **说明生效时间**：如"自 2024 年 1 月 20 日起施行"（须能从官方文件核实）。
3. **更新受影响的文件**：除 `references/` 文档外，凡写进脚本的阈值、字段或检查项，必须同步改脚本与固件测试。

用 Issue 模板"规则更新"提交，或直接发 PR（附上述信息）。

### 1.4 时效性声明

凡引用会随时间变化的内容（费用标准、减缴比例、法定期限、表格版本），必须写明：

- 该数字对应的**官方文件名称与条款**；
- **检索日期**（例：`检索日期 2026-01-15`）；
- 一句"以 CNIPA 官网最新公布为准"的提示。

---

## 二、技术贡献

### 2.1 改进 `check_patent.py`

- 新增检查项须有**明确依据**（法条、细则条款或审查指南原文）。
- 每个 FAIL 级检查必须附 `hint`（说明依据与修正方向）。
- 添加固件到 `SELF_TEST_CASES`，确保 `--self-test` 通过。
- WARN 级检查可基于公开实务惯例（需注明"非强制"）。

### 2.2 改进 `check_latex.py`、`make_docx.py` 或其它脚本

- 除 LaTeX 编译链外，脚本必须保持**纯标准库**（不添加外部依赖）；`make_docx.py` 只能用标准库生成 OOXML。
- 退出码语义明确：0=通过，1=错误，2=使用错误。
- 非交互式（CI 友好）。
- **不允许**为了通过而放宽阈值（页数下限、PDF 体积下限、必需章节清单）。真编不过就修模板。

### 2.3 添加 `references/` 文档或 `assets/`

- 新文档须在 `SKILL.md` 的"参考文件索引"表中加一行。
- 外部链接必须是稳定的（CNIPA 官网 > 官方法律数据库 > GitHub releases）。
- 模板须可直接编译/打开，并在 `assets/latex/README.md` 或 `assets/README.md` 中登记。

### 2.4 不写"法律意见"

本技能可以整理规则、给检查清单、生成模板、做机械自检，但**不得输出**：

- 对具体申请能否授权的断言（"这件一定能授权"）；
- 替代执业专利代理师的结论性意见；
- 规避审查的"技巧"（如教用户规避明显不符合专利法第 26 条第 3 款支持要求的写法）。

---

## 三、流程

### 3.1 报告问题

用 Issue 模板"规则更新"或"Bug 报告"提交；描述问题时请附：

- **具体文件与行号**（如 `references/claims.md:42`）
- **官方原文链接或截图**（规则/费用类问题必须）
- **期望行为 vs 实际行为**（Bug 类问题必须）

### 3.2 提交 Pull Request

1. Fork 本仓库，在你的分支改动。
2. **运行本地检查**（必须全部通过才能提 PR）：

   ```bash
   python scripts/validate_skill.py . --strict
   python scripts/check_patent.py --self-test
   python scripts/check_latex.py --self-test
   python scripts/make_docx.py --self-test
   python scripts/install_skill.py --self-test
   ```

   改了 `examples/draft-example.md` 之类范例时，再跑一次机械自检确认它仍能过：

   ```bash
   python scripts/check_patent.py examples/draft-example.md --strict
   ```

   有 TeX 发行版时再跑一次真编译：`python scripts/check_latex.py`。

3. 提交信息格式：`feat/fix/docs: 简短描述（≤50 字符）`
4. PR 描述须说明：改了什么、为什么改、依据是什么（附链接）。

### 3.3 代码风格

- Python 遵循 PEP 8（允许 100 字符行宽）。
- 模块级 docstring 字段顺序固定为：`参数:`、`返回:`、`算法:`、`复杂度:`、`陷阱:`、`参考:`。
- 不在随包发布的模块里用裸 `assert`（会被 `-O` 优化掉）；用 `raise ValueError` / `raise AssertionError`。
- Markdown 用中文标点（，。；：）、中英文之间留一个空格。
- 文档里的路径一律用正斜杠（Windows 反斜杠路径在别的平台会失效，CI 会拦）。
- 文件末尾保留一个空行。

---

## 四、不接受的贡献（明确拒绝，避免浪费你的时间）

- ❌ **编造的"审查标准""授权率""审查周期"**：官方未公布就诚实写"未公布/未核实"。
- ❌ **"我觉得应该……"的建议**：除非有法条、指南依据或可核实的官方口径。
- ❌ **添加外部依赖**：`check_patent.py`、`validate_skill.py`、`make_docx.py` 必须保持纯标准库。
- ❌ **代写完整申请文件并承诺可用**：本技能的定位是"规则助手 + 撰写脚手架 + 自检工具"。
- ❌ **闭源/付费内容的整合**：必须是可公开验证的资料。
- ❌ **教用户规避法律要求**：包括隐瞒在先技术、编造技术效果、拆分申请以规避单一性审查等。

---

## 五、维护者承诺

- **24 小时内响应**有效的规则变更 Issue（含官方链接）。
- **7 天内合并**符合上述原则的 PR（或给出具体修改建议）。
- **法律、细则、审查指南或收费标准修订生效后尽快更新**受影响的 `references/` 与脚本，
  并在 `CHANGELOG.md` 中注明依据文件与生效日期。

---

## 六、免责声明

本技能不是法律意见，也不替代执业专利代理师。用户依据本技能生成的文件必须自行核对，
并建议在正式提交前交由有资质的代理机构或代理人复核。

---

感谢你帮助这个技能保持准确与时效！
